{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | grep -q 'src/lib/db/schema/' && pnpm db:generate 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Generating database migration..."
          }
        ]
      }
    ]
  }
}
